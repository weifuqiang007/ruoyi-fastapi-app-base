# 打包说明（Windows / macOS / iOS / Android）

> 本文是本项目的**打包能力（Build & Release）规范**。四端打包的入口、脚本、配置、流程全部固化在这里——**以后每次发版，照本文执行即可**，不再临时摸索。
>
> 维护：每次改 `electron/`、`manifest.json`、`package.json` 的版本/构建字段后，同步更新本文。

---

## 0. 能力总览（一张图）

```
                          ┌─────────────────────────────────────┐
   一套 uni-app 源码       │  打包能力层（本项目已固化）          │
   (src/ + electron/)  ──▶ │                                     │
                          │  桌面线（Electron 壳 + H5 产物）     │
                          │   ├─ electron/main.cjs / preload.cjs │
                          │   ├─ electron/build-win.sh  → .exe   │
                          │   ├─ electron/build-mac.sh  → .dmg   │
                          │   └─ .github/workflows（CI 双平台）  │
                          │                                     │
                          │  移动线（uni-app 原生壳，HBuilderX） │
                          │   ├─ manifest.json (appid/权限/Push) │
                          │   └─ 云打包 → .apk / .ipa            │
                          └─────────────────────────────────────┘
                                          │
                ┌────────────┬────────────┼────────────┬────────────┐
                ▼            ▼            ▼            ▼
            Windows       macOS        Android       iOS
            .exe          .dmg         .apk          .ipa
            (本机✓)       (需Mac)      (HBuilderX)   (HBuilderX+证书)
```

### 平台矩阵（先看清现实，避免走弯路）

| 平台 | 产物 | 在哪打 | 入口 | 能否 CI |
|---|---|---|---|---|
| **Windows** | `.exe`（nsis 安装包 + portable） | 任意系统（本机 Windows 已验证） | `bash electron/build-win.sh` | ✅ |
| **macOS** | `.dmg` + `.zip`（arm64） | **仅 macOS** 主机 | `bash electron/build-mac.sh` | ✅（CI 的 mac runner） |
| **Android** | `.apk` | HBuilderX 云打包（任意系统）或 本地 Android SDK（Windows） | HBuilderX GUI | ❌（需 DCloud 登录） |
| **iOS** | `.ipa` | HBuilderX 云打包 或 本地 Xcode（**仅 Mac**）；**必需 Apple 证书** | HBuilderX GUI | ❌ |

> 核心事实：`npm run build:app` / `uni build -p app` 只产出 JS 资源，**不是**可安装的 apk/ipa。移动端必须由 HBuilderX 套原生壳。桌面端 `npm` 可直接出 exe/dmg。

---

## 1. 发版前：必改的版本号

发版前统一改两处（桌面读 package.json，移动读 manifest.json）：

| 文件 | 字段 | 示例 |
|---|---|---|
| `package.json` | `version` | `"1.9.0"` |
| `src/manifest.json` | `versionName` / `versionCode` | `"1.9.0"` / `"100"`（每次发版 code +1） |

> 桌面包文件名会自动带版本号（如 `LawyerCalendar Setup 1.9.0.exe`）。

---

## 2. Windows 打包（.exe）

**本机可直接执行**（已验证产出 118MB 安装包）。

### 一键命令
```bash
npm run app:win          # 等价于 bash electron/build-win.sh
```
脚本会自动：临时移除 `packageManager`（绕开 pnpm×electron-builder 不兼容）→ `build:h5` → `electron-builder --win nsis portable` → 还原 `packageManager`。

### 产物
```
release-win2/LawyerCalendar Setup 1.9.0.exe      # NSIS 安装包（推荐分发）
release-win2/LawyerCalendar-1.9.0-portable.exe   # 免安装便携版
release-win2/win-unpacked/LawyerCalendar.exe     # 解压版（调试用）
```

### 关键配置（`package.json` 的 `build` 字段）
- `appId: com.lawyer.calendar`
- `productName: LawyerCalendar`（ASCII，避免中文路径编码问题；中文窗口标题/快捷方式在 main.cjs / nsis.shortcutName 里）
- `electronDist: node_modules/electron/dist`（**复用本地 electron，跳过下载+解压**，绕开 Windows Defender 锁文件的 EPERM 顽疾）
- `win.target`: nsis + portable
- `nsis`: 非一键安装、可选目录、建桌面+开始菜单快捷方式（名「律师案件管理日历」）

### 已知坑（脚本已处理）
1. `npm i` 用 `--legacy-peer-deps`（vite-plugin-uni peer 冲突）。
2. electron 二进制需手动 `node node_modules/electron/install.js`（设 `ELECTRON_MIRROR=npmmirror`）。
3. EPERM rename → 用 `electronDist` 复用本地 dist 规避。
4. pnpm 收集器解析失败 → 脚本临时移除 `packageManager`。
5. `default_app.asar` 被锁删不掉 → 输出到全新目录 `release-winN`。

---

## 3. macOS 打包（.dmg / .zip）

**必须在 macOS 上执行**（electron-builder 不支持跨平台打 mac 包）。本机 Windows 打不了。

### 本机有 Mac
```bash
npm run app:mac          # 等价于 bash electron/build-mac.sh，产出 release-mac/*.dmg + .zip
```

### 没有_mac → 用 CI
push 到 GitHub，`.github/workflows/build-desktop.yml` 会在 `macos-latest` runner 自动构建，下载产物 `.dmg`。

### 关键配置
- `mac.target`: dmg + zip，arch `arm64`（Apple 芯片，当今主流；要 Intel 改 `["arm64","x64"]`）
- `mac.category`: `public.app-category.productivity`
- `mac.hardenedRuntime: true`
- `dmg`: 含拖到 `/Applications` 的布局

### 行为差异（重要）
- macOS **不允许后台 app 把窗口抢到当前桌面**。到点提醒 = 系统**通知横幅 + Dock 图标关键级跳动**，**不会**像 Windows 那样盖屏。代码里 `app.dock.bounce('critical')` 已做到 Mac 允许的上限。这是系统规则，非 bug。
- **未签名**：首次打开「系统设置 → 隐私与安全性 → 仍要打开」。正式分发需 Apple Developer（$99/年）做 codesign + notarize。

---

## 4. Android 打包（.apk）

**走 HBuilderX 云打包**（最快，免费额度，任意系统）。npm 打不出 apk。

### 步骤
1. 装 **HBuilderX**：https://www.dcloud.io/hbuilderx.html
2. 文件 → 打开目录 → 选本项目根目录 `g:\ruoyi-fastapi-app`。
3. **发行 → 原生 App-云打包**。
4. 勾选 **Android**：
   - 包名：`com.lawyer.calendar`（自定）
   - 证书：**「使用公共测试证书」**先试（仅自测，不能上架）；正式分发要自建 keystore。
   - 权限/模块已在 `manifest.json` 配好（含 Android 13 的 `POST_NOTIFICATIONS`）。
5. 等 3–10 分钟 → 下载 `.apk` → 手机安装。

### 想先不打包、在手机上试运行
HBuilderX → **运行 → 运行到手机或模拟器 → 真机运行**（USB/无线）→ 手机直接跑当前代码，热刷新。**验证提醒功能最快**。

### 为什么手机端提醒体验最好
App 端走 `plus.push.createMessage({delay})` 系统本地通知：**App 被系统杀掉照样到点响**，横幅盖在微信/任何 app 上 + 震动响铃。这是桌面 Electron（关进程就废）和 Mac（不能抢焦点）都比不上的。代码 `scheduleLocal` 的 `#ifdef APP-PLUS` 分支已就绪。

---

## 5. iOS 打包（.ipa）

**最难**：必需 Apple Developer 账号（$99/年），无论云打包还是本地。

### 前置（必须先办）
1. Apple Developer 账号：https://developer.apple.com/programs/
2. 在 Apple 后台创建：
   - **App ID**（Bundle ID，如 `com.lawyer.calendar`）
   - **推送证书** + **开发/发布证书**（.cer → 导出 .p12 + 密码）
   - **Provisioning Profile**（.mobileprovision，绑定 App ID + 证书 + 设备）

### 云打包（推荐，任意系统）
HBuilderX → 发行 → 原生 App-云打包 → 勾选 **iOS** → 上传 `.p12` + `.mobileprovision` + 填 Bundle ID → 打包 → 下载 `.ipa`。

### 本地打包（仅 Mac）
HBuilderX → 发行 → 原生 App-本地打包 → 生成的工程用 **Xcode** 打开 → 选设备/Archive → 出 ipa。

### 安装到真机
`.ipa` 只能装到 profile 里登记的设备（非上架情况）。上架走 App Store Connect + TestFlight（内测）或正式提审。

---

## 6. CI 自动打包（桌面端）

`.github/workflows/build-desktop.yml`：push 到 master/main（或手动触发）→ 在 `windows-latest` 和 `macos-latest` 上分别构建 → 上传 exe/dmg/zip 为 artifact。

- **能自动化**：Windows exe、macOS dmg。
- **不能自动化**：Android apk、iOS ipa（需 DCloud 账号 / Apple 证书，HBuilderX 云打包无公开 API）。

> 要让 CI 也能出 mac 签名包，把 Apple 证书配进 repo Secrets，并在 workflow 里加 codesign/notarize 步骤（后续按需加）。

---

## 7. 发版标准 Checklist（每次发版照此走）

```
□ 1. 改版本号：package.json(version) + manifest.json(versionName/versionCode)
□ 2. 桌面 H5 构建 + 本地冒烟：bash electron/build-win.sh → 跑一下 LawyerCalendar.exe 确认开窗
□ 3. 分发 Windows：上传 release-win2/LawyerCalendar Setup X.X.X.exe
□ 4. macOS：push 触发 CI → 下载 dmg；或 Mac 本机 build-mac.sh
□ 5. Android：HBuilderX 云打包（公共测试证书或正式 keystore）→ 自测安装
□ 6. iOS：办/续 Apple 证书 → HBuilderX 云打包 → 真机/TF
□ 7. （可选）在 release 说明里写版本变更
□ 8. git 提交版本号与 manifest 变更，打 tag v1.X.0
```

---

## 8. 关键文件索引

| 文件 | 作用 |
|---|---|
| [electron/main.cjs](electron/main.cjs) | 桌面主进程：装载 H5、窗口、提醒强制置顶/dock 跳动、`backgroundThrottling:false` |
| [electron/preload.cjs](electron/preload.cjs) | 桌面预载：`window.desktop.focusReminder()` IPC |
| [electron/build-win.sh](electron/build-win.sh) | Windows 一键打包（含 packageManager 临时移除/还原） |
| [electron/build-mac.sh](electron/build-mac.sh) | macOS 一键打包（仅 Mac 可跑） |
| [.github/workflows/build-desktop.yml](.github/workflows/build-desktop.yml) | 桌面 CI（win+mac 矩阵） |
| [package.json](package.json) `build` 字段 | electron-builder 配置（appId/target/electronDist/nsis/mac/dmg） |
| [src/manifest.json](src/manifest.json) | uni-app 配置：appid、Android 权限、Push 模块、iOS、H5 端口 |
| `src/utils/local_reminder.js` | 提醒引擎：H5/Electron 走 Web Notification+IPC；App 走 plus.push |

---

## 9. 常见问题

**Q：Windows 打包报 EPERM rename？**
A：Defender 锁 235MB electron.exe。已用 `electronDist` 复用本地 dist 规避；若仍现，换全新输出目录名（改 `build.directories.output`）。

**Q：electron-builder 报 "macOS targets can only be built on macOS"？**
A：mac 包只能在 Mac 上打。用 CI 或借 Mac。

**Q：npm 打不出 apk/ipa？**
A：uni-app 真机包必须 HBuilderX 云打包或本地 SDK。npm 只出 JS 资源。

**Q：iOS 云打包提示证书错误？**
A：.p12 + .mobileprovision 的 Bundle ID 必须一致，且 profile 里登记了你的设备 UDID。

**Q：Android 装上不弹通知？**
A：Android 13+ 需运行时授权 `POST_NOTIFICATIONS`（已加权限，首次启动由 plus.push 触发系统授权弹窗）；检查系统通知权限是否被关。

**Q：桌面提醒最小化后不弹？**
A：已修（`backgroundThrottling:false` + focus-reminder 置顶）。前提是**进程没被完全退出**——Electron 是普通桌面程序，完全关闭就不响（要"关掉也响"需注册系统计划任务/后台服务，另一套工程）。
